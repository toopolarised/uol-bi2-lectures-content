%\documentclass[convert={density=300,size=480x480,outext=.png}]{standalone}
\documentclass{standalone}
\usepackage[utf8]{inputenc}
\usepackage{tikz}
\usepackage{pgfplotstable}
\usepackage{pgfplots}

\begin{document}
	
% style of the mark
\tikzset{mark options={mark size=0.5, line width=1pt}}

% grid style
%\pgfplotsset{grid style={dashed,gray}}
%\pgfplotsset{minor grid style={dashed,red}}
\pgfplotsset{major grid style={dotted,green!50!black}}

%\pgfplotsset{every tick label/.append style={font=\tiny}}
%\pgfplotsset{every x tick label/.append style={font=\tiny, yshift=0.1ex}}
%\pgfplotsset{every y tick label/.append style={font=\tiny, xshift=0.1ex}}

\pgfplotsset{every axis/.append style={
		axis x line=middle,    % put the x axis in the middle
		axis y line=middle,    % put the y axis in the middle
%		axis line style={<->}, % arrows on the axis
		xlabel={$x$},          % default put x on x-axis
		ylabel={$y$},          % default put y on y-axis
		label style={font=\tiny},
		tick label style={font=\tiny}  
	}}


\vspace{2cm}

\begin{tikzpicture}
	   
	\begin{axis}[
		 xlabel=Average Number of Rooms Per Housing,
		 ylabel=House Price (thousands),
		 grid=both,
		       xmin=3,
		       xmax=9.5,
		       ymin=-1,
		       ymax=57,
		       %xtick={1.0,2.0,...,10.0},
		       %minor xtick={0.5,1.5,...,9.5},
		       %ytick={1.0,2.0,...,60.0},
		       %minor ytick={0.5,1.5,...,59.5},
		  ]
		\addplot [orange, only marks] table [y=PRICE, x=RM]{data/boston-housing-data.csv};
		
		\draw [blue, line width=1.0] (50,50) -- (500,500);
		\draw [gray, line width=1.0] (50,100) -- (400,500);
		\draw [red, line width=1.0] (50,200) -- (600,200);
	\end{axis}
\end{tikzpicture}

\vspace{2cm}

\end{document}